jQuery(document).ready(function() 
{
	/*
	// run actual input field image when click on span button
	jQuery('.evoau_video_btn').on('click',function(){
		$(this).parent().find('input#my_video_upload').click();
	});
	*/
	/*
	// run actual input field image when click on span button
	jQuery('.evoau_img_btn').on('click',function(){
		$(this).parent().find('input').click();
	});
	*/
	/*
	// remove existing images
	jQuery('body').on('click','.evoau_video_preview span',function(){
		$(this).parent().fadeOut();
		$(this).parent().next().fadeIn();
		$(this).siblings('input').val('no');
	});
	*/
});